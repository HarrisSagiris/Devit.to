<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Bookmarks | Devit.to</title>
    <link rel="stylesheet" href="/styles.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
</head>
<body>
    <div class="version-banner">🔖 My Bookmarked Posts</div>

    <header class="header">
        <div class="header-content">
            <a href="/" class="logo">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                    <path d="M12 2L2 7L12 12L22 7L12 2Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 17L12 22L22 17" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M2 12L12 17L22 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
                Devit.to
            </a>
            <% if (user) { %>
                <div class="nav-links">
                    <div class="profile-dropdown">
                        <div class="profile-trigger">
                            <img src="<%= user.avatar || '/icons/icon.jpg' %>" alt="Profile" class="profile-avatar">
                            <i class="fas fa-chevron-down"></i>
                        </div>
                    </div>
                </div>
            <% } %>
        </div>
    </header>

    <main class="container">
        <div class="feed">
            <% if (bookmarkedPosts && bookmarkedPosts.length > 0) { %>
                <% bookmarkedPosts.forEach(post => { %>
                    <div class="post" id="post-<%= post._id %>">
                        <div class="vote-buttons">
                            <button class="vote-btn upvote <%= post.upvotedBy?.includes(user?._id) ? 'active' : '' %>" data-id="<%= post._id %>">⬆️</button>
                            <span class="vote-count"><%= post.upvotes - post.downvotes %></span>
                            <button class="vote-btn downvote <%= post.downvotedBy?.includes(user?._id) ? 'active' : '' %>" data-id="<%= post._id %>">⬇️</button>
                        </div>
                        <div class="post-main">
                            <div class="post-header">
                                <div class="post-meta">
                                    Posted in <%= post.category %> by <%= post.username %> • <%= new Date(post.createdAt).toLocaleDateString() %>
                                </div>
                                <h3 class="post-title"><%= post.title %></h3>
                                <div class="post-content"><%= post.content %></div>
                            </div>
                            <div class="post-actions">
                                <button class="comments-toggle">💬 <%= post.comments.length %> Comments</button>
                                <button class="bookmark-btn bookmarked" data-id="<%= post._id %>" onclick="toggleBookmark(this, '<%= post._id %>')">
                                    🔖 Bookmarked
                                </button>
                            </div>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="no-bookmarks">
                    <h2>No bookmarked posts yet!</h2>
                    <p>Browse posts and bookmark the ones you'd like to save for later.</p>
                    <a href="/" class="primary-btn">Browse Posts</a>
                </div>
            <% } %>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2025 DEVIT.TO. All rights reserved.</p>
        <p>Licensed under @devharris</p>
    </footer>

    <script>
        function toggleBookmark(btn, postId) {
            $.ajax({
                url: `/post/${postId}/bookmark`,
                method: 'POST',
                success: function(response) {
                    if (response.success) {
                        $(btn).closest('.post').fadeOut(400, function() {
                            $(this).remove();
                            if ($('.post').length === 0) {
                                location.reload();
                            }
                        });
                    }
                },
                error: function() {
                    alert('Error toggling bookmark');
                }
            });
        }
    </script>
</body>
</html>
